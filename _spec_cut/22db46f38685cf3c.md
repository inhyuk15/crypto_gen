1 

# EMBLEM: (Ring) LWE-based Key Encapsulation With a New Multi-bit Encoding Method 

Minhye Seo, Suhri Kim, Dong Hoon Lee, and Jong Hwan Park 

**_Abstract_ —Lattice-based cryptography is a promising candidate for post-quantum cryptosystems, and a large amount of research has been conducted on learning with errors (LWE) problems, which are believed to be resistant against quantum attacks. In this paper, two new key encapsulation mechanisms (KEMs) are proposed, called EMBLEM and R.EMBLEM, based on (ring) LWE problems. The new KEMs have two main features: 1) Their security is based on the (ring) LWE problem with small secrets, which leads to both a secret key of constant size (regardless of the LWE parameters) and a relatively large standard deviation of the discrete Gaussian distributions. 2) They rely on a new multi-bit encoding method that is suitable for (ring) LWE-based encryption schemes. Compared to Regev’s encoding method, the proposed method does not require any rounding operation for decoding, and in this sense, it is conceptually simpler and easier to understand. Concrete parameters of the KEMs targeting 128-bit security level (against classical attacks) are provided, and their performance is compared with that of previous (ring) LWE-based KEMs in the literature.** 

**_Index Terms_ —lattice-based cryptography, chosen-ciphertext security, key encapsulation mechanism, small secret LWE** 

## _A. Contributions_ 

In this paper, we propose two new chosen-ciphertext-secure KEMs whose security relies on small secret (ring) LWE. The KEMs follow the same structural design as the LP construction, but there are two major differences: First, our basic construction is designed for encrypting multi-bit messages at a time by using small secret LWE. The elements of the vectors **r** and **x** in the proposed schemes are uniformly chosen from [ _−B, B_ ] at random for a small positive integer _B_ , such as _B_ = 1, and the size of the modulus _q_ is moderately increased to encrypt _t_ -bit message as desired. By using small secret LWE, the proposed KEMs naturally have two advantages, namely, the constant size of the secret key and the quite large standard deviation of _GDs_ . Indeed, the size of the secret key is always 32 bytes in all parameter settings of the proposed KEMs and the standard deviation of _GDs_ can be set to _s/√_ 2 _π_ = 25 in the proposed LWE-based KEM. Moreover, by running the basic scheme in parallel, an encryption scheme can be constructed that handles at least 256-bit messages and is proved to be chosen-plaintext-secure under (ring) LWE. By applying the KEM variant of the Fujisaki–Okamoto (FO) 

## _B. Overview of proposed encoding method_ 

Fig. 1 shows the proposed encoding method for _t_ -bit encryption within a modulus _q_ . It should be noticed that for a given modulus _q_ , an element in Z _q_ is represented as _⌊_ log2 _q⌋_ + 1 bits, and thus any _⌊_ log2 _q⌋_ -bit number belongs to Z _q_ . The encoding is performed within _⌊_ log2 _q⌋_ bits in Z _q_ . It is assumed that a 256-bit message is represented by a _v × k_ matrix **M** = _{M_ ( _i,j_ ) _}_ for some _v, k_ such that _t × v × k_ = 256, where each entry _M_ ( _i,j_ ) is _t_ bits. By using the inequality _|_ **r**<sup>**T**</sup> **e** + **e2** _−_ **e1x** _| <_ 2<sup>_d_</sup> , the size _d_ can be estimated in advance when the parameters are set, so as to ensure a negligible correctness error. Then, the length _t_ of an encoded message is determined by the equation _⌊_ log2 _q⌋_ = _t_ + 1 + _d_ . Each _t_ -bit message _M_ ( _i,j_ ) is encoded within the modulus _q_ in the form of _M_ ( _i,j_ ) _||_ 1 _||_ 0<sup>_d_</sup> , where the bit 1 in the middle is what is called “error-blocking bit”. The resulting encoded messages are then reconstructed into a matrix **∆** _∈_ Z<sup>_v_</sup> _q_<sup>_×k_</sup> again. 

In the decryption phase, given the (matrix form of) ciphertext ( **C1** _,_ **C2** ) and the secret key **X** , **C2** _−_ **C1X** = 

3 

**E** (Decryption error) + **∆** (Encoded message) is computed, where each ( _i, j_ )-entry of **C2** _−_ **C1X** is in the form ( **r**<sup>**T**</sup> **e** + **e2** _−_ **e1x** ) + _M_ ( _i,j_ ) _||_ 1 _||_ 0<sup>_d_</sup> . If the error term _ε_ is _positive_ and less than 2<sup>_d_</sup> , the error is added into the least significant _d_ bits and has no effect on the message _M_ ( _i,j_ ), whereas if the error term _ε_ is _negative_ and its absolute value is less than 2<sup>_d_</sup> , it affects beyond the least significant _d_ bits. However, owing to the error-blocking bit 1, the negative error is computed as a positive value 2<sup>_d_</sup> + _ε_ ( _<_ 2<sup>_d_</sup> ), and thus it is not propagated to the message part. Fig. 2 shows the cases of positive or negative error. As a result, any positive or negative error does not affect the message part as long as _|_ **r**<sup>**T**</sup> **e** + **e2** _−_ **e1x** _| <_ 2<sup>_d_</sup> . 

<!-- Start of picture text -->
� � ������<br>�����<br>� �������������������<br>������ � � �<br>�� ��� � � � � ��<br><!-- End of picture text -->

Fig. 2: Positive or negative error in decryption 

## _D. Organization_ 

The remainder of this paper is organized as follows: In Section II, the basic notions are introduced, and the definitions of public-key encryption (PKE) and KEM, as well as their security models, are reviewed. In Section III, the new chosenplaintext-secure PKE scheme and chosen-ciphertext-secure KEM based on the small secret LWE problem are presented. In Section IV, the ring variants of the constructions are presented. Section V is dedicated to analyzing the parameter selection. Finally, the subroutines of the implementation are described in Section VI, and the performance of the proposed schemes is analyzed in Section VII. 

## II. BACKGROUND 

## _A. Notation_ 

The set of natural numbers is denoted by N and the set of integers by Z. For _q ∈_ N, the set Z _q_ is defined as Z _∩_ ( _−_ 2<sup>_<u>q</u>,_</sup> 2<sup>_<u>q</u>_].</sup> For a finite set _S_ , the notation _a ←R S_ implies that _a_ is chosen uniformly at random from _S_ and for a distribution _X_ , we write _a ←X_ to denote that _a_ is sampled according to the distribution _X_ . For a matrix **M** , **M**<sup>_T_</sup> denotes the transpose of **M** . For a vector _a_ of length _n_ , _ai_ denotes the _i_ -th component of _a_ , and for a matrix **M** , **M** [ _i, j_ ] is the _i_ -th row and _j_ - th column entry of **M** . The Euclidean norm _||a||_ is defined as �Σ<sup>_n_</sup> _i_ =1<sup>_a_2</sup> _i_<sup>.Forabitstring</sup><sup>_b_,afunction[</sup><sup>_b_]</sup><sup>_t_isdefined</sup> that outputs the most significant _t_ bits of _b_ . This function is extended to matrices by applying it to each component of a matrix. For positive integers _q_ and _n_ , _U_ (Z<sup>_n_</sup> _q_<sup>)isdefinedby</sup> the uniform distribution over Z<sup>_n_</sup> _q_<sup>.Throughoutthispaper,log</sup> is the logarithm with base 2. 

## _B. Discrete Gaussian Distribution_ 

For a given _s >_ 0, the _discrete_ Gaussian distribution over a lattice _L_ is as 

4 

for any _x ∈ L_ , where _ρ_ denote the Gaussian function _ρs_ ( _x_ )= _e_<sup>_−π∥x∥_2</sup><sup>_/s_2</sup> . 

Note that the standard deviation of _GDL,s_ is _σ_ = _s/√_ 2 _π_ . The Gaussian parameter _s_ is used to describe a discrete Gaussian distribution. _GDs_ denotes the discrete Gaussian distribution _GD_ Z _,s_ . Moreover, it holds that _GD_ Z _n,s_ = _GD_<sup>_n_</sup> _s_<sup>.</sup> 

## _C. Learning with Errors_ 

The learning with errors (LWE) problem with multiple secrets will now be defined, which has been proven to be at least as hard as the standard LWE problem [8]. 

**Definition 2.1** (Decision LWE problem). Let _m, n, k, q ∈_ N and _Ds, De_ be distributions over Z _q_ . One is given an instance ( **A** _,_ **B** ) _∈_ Z<sup>_m_</sup> _q_<sup>_×n_</sup> _×_ Z<sup>_m_</sup> _q_<sup>_×k_</sup> and is asked to distinguish whether there exists **S** _←Ds_<sup>_n×k_</sup> such that the instance is of the form ( **A** _,_ **AS** + **E** mod _q_ ) with **A** _←R_ Z<sup>_m_</sup> _q_<sup>_×n_</sup> , **E** _←De_<sup>_m×k_</sup> or the instance is uniformly chosen at random from Z<sup>_m_</sup> _q_<sup>_×n_</sup> _×_ Z<sup>_m_</sup> _q_<sup>_×k_</sup> . The decision LWE problem is denoted by LWE _n,m,q,De_ . 

The binary-LWE problem (where the secret matrix **S** is randomly chosen from _{−_ 1 _,_ 0 _,_ 1 _}_<sup>_n×k_</sup> ) has been considered in [45], [46]. In [9], Bai and Galbraith proved that the binaryLWE problem is as hard as the standard LWE problem as long as the parameter _n_ is increased by a factor of log(log _n_ ). Note that the errors are still chosen from discrete Gaussian distributions. Albrecht _et al._ [10] applied the embedding technique of Bai and Galbraith [9] to the generalized cases, where the elements of **S** are randomly sampled from a small interval. Here, [10] is applied to the case where **S** is sampled from [ _−B, B_ ]<sup>_n×k_</sup> for any positive integer _B < σ_ . The decisional small secret LWE problem, denoted by smaLWE _n,m,q,De_ , is as follows. 

**Definition 2.2** (Decision LWE Problem with small secrets [45]). Let _m, n, k, q ∈_ N and _De_ be a distribution over Z _q_ . One is given an instance ( **A** _,_ **B** ) _∈_ Z<sup>_m_</sup> _q_<sup>_×n_</sup> _×_ Z _q_<sup>_m×k_</sup> and is asked to distinguish whether there exists **S** _←R_ [ _−B, B_ ]<sup>_n×k_</sup> such that the instance is of the form ( **A** _,_ **AS** + **E** mod _q_ ) with **A** _←R_ Z<sup>_m_</sup> _q_<sup>_×n_</sup> , **E** _←De_<sup>_m×k_</sup> or the instance is uniformly chosen at random from Z<sup>_m_</sup> _q_<sup>_×n_</sup> _×_ Z<sup>_m_</sup> _q_<sup>_×k_</sup> . 

**Lemma 2.3** (Log root Hermite factor of LWE instances of small secret [10]). Let a small secret LWE instance be characterized by _n, α, q_ , let **s** ( _i_ ) _←R_ [ _−B, B_ ], let _ξ_ = 1 _/B_ , and let _σ_ = _αq_<sup>Anylatticereductionalgorithmachieving</sup> _~~√~~_ 2 _π_<sup>.</sup> _log root-Hermite factor_ 

Lyubashevsky _et al._ [28] proposed the ring-LWE problem over rings. The notation _a ←X_<sup>_n_</sup> implies that _n_ coefficients of a polynomial _a_ are chosen independently from _X_ . 

**Definition 2.4** (Decision Ring-LWE problem). Let _n, q ∈_ N and _Ds, De_ be distributions over Z _q_ . For an irreducible polynomial _f_ ( _x_ ) _∈_ Z[ _x_ ] of degree _n_ , let _Rq_ = Z _q_ [ _x_ ] _/f_ ( _x_ ) be the ring modulo _q_ . One is given ( _a, b_ ) _∈ Rq_<sup>2andisaskedto</sup> distinguish whether there exists _s ←Ds_<sup>_n_suchthat(</sup><sup>_a, b_)isof</sup> the form ( _a, a · s_ + _e_ ) with _a ←R Rq_ , _e ←De_<sup>_n_or(</sup><sup>_a, b_)is</sup> chosen uniformly at random from _Rq_<sup>2. The decision Ring-LWE</sup> problem is denoted by RLWE _n,q,De_ . 

The Ring-LWE problem with small secrets (where the coefficients of the secret polynomial _s_ are randomly chosen from [ _−B, B_ ]<sup>_n_</sup> ) can be defined as in the case of smaLWE. In [47], it is shown that for the standard LWE, the secret _s_ can be sampled from any distribution as long as its entropy is sufficiently large. Under the assumption that the analysis in the standard LWE setting equally holds in the ring-LWE setting, the coefficients of the secret polynomial can be set to be small in the ring-LWE problem. The decisional small secret Ring-LWE problem, denoted by smaRLWE _n,q,De_ , is defined as follows. 

**Definition 2.5** (Decision Ring-LWE Problem with small secrets). Let _n, q ∈_ N and _De_ be a distribution over Z _q_ . For an irreducible polynomial _f_ ( _x_ ) _∈_ Z[ _x_ ] of degree _n_ , let _Rq_ = Z _q_ [ _x_ ] _/f_ ( _x_ ) be the ring modulo _q_ . One is given ( _a, b_ ) _∈ Rq_<sup>2andisaskedtodistinguishwhetherthereexists</sup> _s ←R_ [ _−B, B_ ]<sup>_n_</sup> such that ( _a, b_ ) is of the form ( _a, a · s_ + _e_ ) with _a ←R Rq_ , _e ←De_<sup>_n_or(</sup><sup>_a, b_)isuniformlychosenat</sup> random from _R_<sup>2</sup> _q_<sup>.</sup> 

## _D. Definitions_ 

**Definition 2.6** (Public Key Encryption). A public-key encryption (PKE) scheme consists of the following three algorithms: KeyGen, Encrypt, and Decrypt. 

- KeyGen�1<sup>_λ_�</sup> : The key generation algorithm takes as input the security parameter 1<sup>_λ_</sup> and outputs a public/secret key pair ( _pk, sk_ ). 

- - Encrypt� _pk, M_ �: The encryption algorithm takes as input a public key _pk_ and a message _M ∈M_ . Then it outputs a corresponding ciphertext _C_ . 

- - Decrypt� _sk, C_ �: The decryption algorithm takes as input a secret key _sk_ and a ciphertext _C_ . It outputs a message _M_ or _⊥_ (which indicates decryption failure). 

**_Correctness._** The correctness of a PKE scheme is ensured if the following condition holds: For all _M ∈M_ , 

where _ϵ_ is a negligible function. 

**Definition 2.7** (Key Encapsulation Mechanism). A key encapsulation mechanism (KEM) consists of the following three algorithms: KeyGen, Encap, and Decap. 

- KeyGen(1<sup>_λ_</sup> ): The key generation algorithm takes as input the security parameter 1<sup>_λ_</sup> and outputs a public/secret key pair ( _pk, sk_ ). 

5 

- Encap( _pk_ ): The encapsulation algorithm takes as input a public key _pk_ . Then it outputs a ciphertext _C_ and a key _K ∈K_ . 

- Decap( _sk, C_ ): The decapsulation algorithm takes as input a secret key _sk_ and a ciphertext _C_ . It outputs a key _K_ or _⊥_ (which indicates decapsulation failure). 

- **_Correctness._** The correctness of KEM is ensured if the following condition holds: 

where _ϵ_ is a negligible function. 

**Definition 2.8** (IND-CPA Security of PKE). Let PKE = (KeyGen, Encrypt, Decrypt) be a public-key encryption scheme. An experiment (parameterized by a bit _b_ ) between an adversary _A_ and a challenger is defined as follows: 

## **Experiment IND-CPA**<sup>_b_</sup> PKE _,A_<sup>(</sup><sup>_λ_):</sup> 

_1. The challenger runs_ ( _pk, sk_ ) _← KeyGen_ (1<sup>_λ_</sup> ) _and gives pk to A_ . 

_2. A outputs two messages_ ( _M_ 0 _, M_ 1) _of the same length_ . 

_3. The challenger computes Encrypt_ ( _pk, Mb_ ) _for a randomly chosen bit b ∈{_ 0 _,_ 1 _} and gives it to A_ . 

_4. A outputs a bit b_<sup>_′_</sup> _. The challenger returns b_<sup>_′_</sup> _as the output of the game._ 

The advantage of _A_ for breaking the IND-CPA security of a PKE is as 

_We say that_ PKE _is IND-CPA secure if for any polynomial time adversary A and any λ, we have_ **Adv**<sup>_IND-CPA_</sup> PKE _,A ≤ ϵ_ ( _λ_ ) _, where ϵ is a negligible function._ 

**Definition 2.9** (IND-CCA Security of KEM). Let KEM = (KeyGen, Encap, Decap) be a key encapsulation mechanism. An experiment (parameterized by a bit _b_ ) between an adversary _A_ and a challenger is defined as follows: 

## **Experiment IND-CCA**<sup>_b_</sup> KEM _,A_<sup>(</sup><sup>_λ_):</sup> 

_1. The challenger runs_ ( _pk, sk_ ) _← KeyGen_ (1<sup>_λ_</sup> ) _and gives pk to A_ . 

_2. A queries the decapsulation oracle Decap_ ( _sk, ·_ ). _3. The challenger computes_ ( _C_<sup>_∗_</sup> _, K_ 0<sup>_∗_)</sup><sup>_←Encap_(</sup><sup>_pk_)</sup> _and K_ 1<sup>_∗←RK.Thenthechallengergives_(</sup><sup>_C∗, K_</sup> _b_<sup>_∗_)</sup> _to A_ for a randomly chosen bit _b ∈{_ 0 _,_ 1 _}_ . 

_4. A continues to query the decapsulation oracle but may not query the ciphertext C_<sup>_∗_</sup> _. Finally, A outputs a bit b_<sup>_′_</sup> _. The challenger returns b_<sup>_′_</sup> _as the output of the game._ 

The advantage of _A_ for breaking the IND-CCA security of KEM is as 

_We say that_ KEM _is IND-CCA secure if for any polynomial time adversary A and any λ, we have_ **Adv**<sup>_IND-CCA_</sup> KEM _,A_<sup>_≤ϵ_(</sup><sup>_λ_)</sup><sup>_,_</sup> _where ϵ is a negligible function._ 

In the (quantum) random oracle model, the adversary additionally issues the (quantum) random oracles. The constructions in this paper will consider the quantum accessible random oracles described in [48]. 

## III. CCA-SECURE KEY ENCAPSULATION MECHANISM 

In this section, we introduce an IND-CCA secure KEM, called EMBLEM. An IND-CPA secure PKE scheme, EMBLEM.CPA, is first constructed, and then it is converted into EMBLEM using the KEM variant [19] of the Fujisaki– Okamoto transformation. 

## _A. EMBLEM.CPA (CPA-Secure PKE)_ 

_•_ **Encoding and decoding functions.** Let _M_ = _{_ 0 _,_ 1 _}_<sup>_ℓ_</sup> be the message space. The encoding function **encode** is first defined; it takes an _ℓ_ -bit string as input and outputs the encoded message in matrix form. The decoding function **decode** is an inverse of **encode** . For an _ℓ_ -bit message _M_ , a block size _t_ , an upper bound _d_ for the error size, positive integers _v_ and _k_ such that _ℓ/t_ = _v × k_ , and a modulus _q_ , **encode** and **decode** are as follows: 

## ▶ **encode** ( _M, t, d, v, k, q_ ) 

- 1) Split the message into _t_ -bit blocks (it is assumed that _t_ divides _ℓ_ ) and generate message blocks _{Mi′}_ for _i_<sup>_′_</sup> _∈_ [1 _, ℓ/t_ ]. 

- 2) Compute _M_ ( _i,j_ ) = _Mi′_ for _i_<sup>_′_</sup> _∈_ [1 _, ℓ/t_ ], where _i_ = _⌈i_<sup>_′_</sup> _/k⌉ ∈_ [1 _, v_ ] and _j_ = _i_<sup>_′_</sup> _−⌊i_<sup>_′_</sup> _/k⌋· k ∈_ [1 _, k_ ]. 

- 3) Output a _v × k_ matrix **M** = � **M** [ _i, j_ ]�, where **M** [ _i, j_ ] _← M_ ( _i,j_ ) _||_ 1 _||_ 0<sup>_d_</sup> for _i ∈_ [1 _, v_ ], _j ∈_ [1 _, k_ ] (as in Fig. 3). 

<!-- Start of picture text -->
���� ����<br>������ ������ �� �������<br>����� ����� �����<br>������ ������ � � � ������<br>� ������ ������ � ������ � ������<br>��� �� � � � � � �<br>������ ������ � � � ������<br><!-- End of picture text -->

Fig. 3: Encoding function **encode** 

## ▶ **decode** ( **M** _, t, q_ ) 

- 1) Compute _Mi′_ = � **M** [ _i, j_ ]� _t_<sup>for</sup><sup>_i∈_[1</sup><sup>_, v_]</sup><sup>_, j∈_[1</sup><sup>_, k_],</sup> where _i_<sup>_′_</sup> = ( _i −_ 1) _k_ + _j ∈_ [1 _, ℓ/t_ ]; 

- 2) Output _ℓ_ -bit string _M_ = _M_ 1 _|| · · · ||Mℓ/t_ . 

_•_ **Sampling function.** The sampling function **Sam** is now defined; it takes as input a random seed _r_ and outputs ephemeral values ( **R** _,_ **E1** _,_ **E2** ), where **R** is a random matrix in [ _−B, B_ ]<sup>_m×v_</sup> and ( **E1** _,_ **E2** ) is randomly sampled from _GDs_<sup>_v×_(</sup><sup>_n_+</sup><sup>_k_)</sup> for some _s_ . The function **Sam** produces a deterministic output for the same input, and the size of the 

6 

outputs ( **R** _,_ **E1** _,_ **E2** ) can be variable depending on a security parameter. In practice, a Gaussian sampler internally selects a random seed and uses it to generate a sampling output. In the **Sam** function, _s_ 1 and _s_ 2 are internally computed as _s_ 1 _←_ PRF( _r||_ 1) and _s_ 2 _←_ PRF( _r||_ 2) and are used for generating **E1** and **E2** , respectively. 

_•_ **Scheme.** Given a security parameter 1<sup>_λ_</sup> , the system-wide parameter _params_ is generated as follows: Positive integers _m_ , _n_ , _k_ , _t_ , _d_ , _v_ and the modulus _q_ are chosen under the constraint that _m > n_ and _m > n_ + _k_ . A standard deviation _σ_ = _s/√_ 2 _π_ for _GDs_ and a positive integer _B < σ_ are chosen. Then, _params_ is given by ( _m, n, k, q, t, d_ , _v, B, GDs_ ). Note that _ℓ/t_ = _v × k_ . It is assumed that _params_ is used for all algorithms in EMBLEM.CPA. 

- KeyGenChoose(1<sup>_λ_</sup> ). a random matrix **A** _←_ Z<sup>_m_</sup> _q_<sup>_×n_</sup> , a secret random matrix **X** _←_ [ _−B, B_ ]<sup>_n×k_</sup> , and an error matrix **E** _←GD_<sup>_m_</sup> _s_<sup>_×k_</sup> . Compute **B** = **AX** + **E** . The key pair ( _pk, sk_ ) is given by _pk_ = ( **A** _,_ **B** ) and _sk_ = ( **X** ).<sup>2</sup> 

EncryptTo( _pk, M_ encrypt _∈M_ a message,). proceed as follows: 

- 1) **M** _←_ **encode** ( _M, t, d, v, k, q_ ). 

- 2) Choose a random seed _r ∈{_ 0 _,_ 1 _}_<sup>256</sup> . 

- 3) ( **R** _,_ **E1** _,_ **E2** ) _←_ **Sam** ( _r_ ), where **R** _∈_ [ _−B, B_ ]<sup>_m×v_</sup> and ( **E1** _,_ **E2** ) _∈GD_<sup>_v_</sup> _s_<sup>_×_(</sup><sup>_n_+</sup><sup>_k_)</sup> . 

- 4) Compute ( **C1** _,_ **C2** ) = ( **R**<sup>_T_</sup> **A** + **E1** _,_ **R**<sup>_T_</sup> **B** + **E2** + **M** ). 

DecryptParse( _sk, C_ the). ciphertext _C_ as ( **C1** _,_ **C2** ). 1) Compute **M** = **C2** _−_ **C1X** . 2) Output _M ←_ **decode** ( **M** _, t, q_ ). 

**Theorem 3.1 (Correctness).** _For simplicity, the case v_ = 1 _is considered. Then_ **R** _∈_ [ _−B, B_ ]<sup>_m_</sup> _,_ **E1** _∈GD_<sup>_n_</sup> _s_<sup>_, and_</sup><sup>**E2**</sup><sup>_∈GDk_</sup> _s_<sup>_._</sup> _For i ∈_ [1 _, k_ ] _, the following are defined: •_ **E** = � _E_<sup>(</sup><sup>_i_)�</sup> , where _E_<sup>(</sup><sup>_i_)</sup> _∈GD_<sup>_m_</sup> _s_<sup>is an</sup><sup>_i_-th column of</sup><sup>**E**.</sup> _•_ **E2** = � _e_<sup>(</sup> 2<sup>_i_)</sup> �, where _e_ 2<sup>(</sup><sup>_i_)</sup> _∈GDs_ is an _i_ -th entry of **E2** . _•_ **X** = � _X_<sup>(</sup><sup>_i_)�</sup> , where _X_<sup>(</sup><sup>_i_)</sup> _∈_ [ _−B, B_ ]<sup>_n_</sup> is an _i_ -th column of **X** . _Let ϵ_ = Pr max _|⟨_ **R** _, E_<sup>(</sup><sup>_i_)</sup> _⟩|_ + _|e_<sup>(</sup> 2<sup>_i_)</sup><sup>_|_+</sup><sup>_|⟨_</sup><sup>**E1**</sup><sup>_, X_(</sup><sup>_i_)</sup><sup>_⟩|_</sup> _≥_ 2<sup>_d_�</sup> _,_ � _i∈_ [1 _,k_ ] � � _where d_ = _⌊_ log _q⌋−_ ( _t_ + 1) _. Then_ EMBLEM _._ CPA _is (_ 1 _− ϵ)correct._ 

_Proof._ The decryption proceeds as follows: 

Note that for _j ∈_ [1 _, k_ ], the _j_ -th entry of ( **R**<sup>**T**</sup> **E** + **E2** _−_ **E1X** ) _∈_ Z<sup>_k_</sup> _q_<sup>becomes</sup><sup>_⟨_</sup><sup>**R**</sup><sup>_, E_(</sup><sup>_j_)</sup><sup>_⟩_+</sup><sup>_e_(</sup> 2<sup>_i_)</sup> _−⟨_ **E1** _, X_<sup>(</sup><sup>_j_)</sup> _⟩_ , and the _j_ -th entry of **M** _∈_ Z<sup>_k_</sup> _q_<sup>isintheform</sup><sup>_Mj||_1</sup><sup>_||_0</sup><sup>_d_,where</sup> 

> 2The secret matrix **X** can be generated using a PRF and a short key _seed_ **X** , so that a user need only store _seed_ **X** instead of the entire matrix **X** . Similarly, the matrix **A** in the public key can also be derived from a seed by using PRF. In [11], AES128-ECB was used as a PRF with a 256-bit seed. 

_Mj ∈{_ 0 _,_ 1 _}_<sup>_t_</sup> is the _j_ -th message block of _M_ . Therefore, if the absolute size of the _j_ -th entry in ( **R**<sup>**T**</sup> **E** + **E2** _−_ **E1X** ) is less than 2<sup>_d_</sup> , it does not affect the message block _Mj_ (mentioned in Section I-B). As a result, the scheme satisfies correctness with probability of 1 _− ϵ_ . □ 

In Section V, the parameters will be set so that the decryption error _ϵ_ may be negligible, i.e., _ϵ_ = 2<sup>_−_140</sup> . 

## **Theorem 3.2 (Security).** _EMBLEM.CPA is IND-CPA secure if the smaLWEn,m,q assumption holds._ 

_Proof._ The proof proceeds by a sequence of games. In Game 0, the public key is a small secret LWE instance and the ciphertext is an encryption of _M_ 0. In Game 5, the public key is a small secret LWE instance and the ciphertext is an encryption of _M_ 1. It will be shown that the distributions between Game 0 and Game 5 are computationally indistinguishable for the adversary. 

_▷_ **Game 0.** This is the real game, where the public key and the ciphertext are generated honestly as in the real construction. In this game, the ciphertext is an encryption of _M_ 0. Note that **M0** _←_ **encode** ( _M_ 0 _, t, d, v, k, q_ ). The distribution of Game 0, denoted by _D_ 0, is given as follows: 

_▷_ **Game 1.** In Game 1, the public key **B** is uniformly generated at random. The remaining part is the same as in Game 0. The distribution of Game 1, denoted by _D_ 1, is given as follows: 

_▷_ **Game 2.** In Game 2, small secret LWE instances in the ciphertext are changed into random matrices. That is, the ciphertext _C_ = ( **C1** _,_ **C2** )= ( **U1** _,_ **U2** + **M0** ), where ( **U1** _,_ **U2** ) is uniformly generated at random in Z<sup>_v_</sup> _q_<sup>_×n_</sup> _×_ Z<sup>_v_</sup> _q_<sup>_×k_</sup> . The remaining part is the same as in Game 1. The distribution of Game 2, denoted by _D_ 2, is given as follows: 

_▷_ **Game 3.** In Game 3, the message _M_ 0 changes to another message _M_ 1. Note that **M1** _←_ **encode** ( _M_ 1 _, t, d, v, k, q_ ). The remaining part is the same as in Game 2. The distribution of Game 3, denoted by _D_ 3, is given as follows: 

_▷_ **Game 4.** In Game 4, the ciphertext is restored to small secret LWE instances, and the remaining part is the same as in Game 3. The distribution of Game 4, denoted by _D_ 4, is given as follows: 

_▷_ **Game 5.** In Game 5, the public key is restored to the small secret LWE instance, and the remaining part is the same as in 

7 

Game 4. The distribution of Game 5, denoted by _D_ 5, is given as follows: 

The distributions _D_ 0 and _D_ 1 are computationally indistinguishable under the smaLWE _n,m,q_ assumption. The distributions _D_ 1 and _D_ 2 are also computationally indistinguishable under the smaLWE _m,n_ + _k,q_ assumption, as the ciphertext in Game 1 forms the small secret LWE instances with ( _n_ + _k_ ) samples in dimension _m_ . Since _m > n_ and _m > n_ + _k_ in the _params_ generation, the smaLWE _n,m,q_ is reduced to the smaLWE _m,n_ + _k,q_ , and thus only the smaLWE _n,m,q_ is sufficient for the security assumption. In Games 2 and 3, the ciphertexts are computed in a one-time pad manner by adding the message to a random matrix, so that the distributions _D_ 2 and _D_ 3 are statistically indistinguishable. The hybrid games, i.e., Games 3–5, proceed as in the case of Games 0–2, but in a reverse manner. If the parameters _n, m_ and _q_ of the smaLWE are set to be as hard as those of LWE, then the security of the standard LWE problem can be reduced to that of EMBLEM.CPA. □ 

## _B. EMBLEM (CCA-Secure KEM)_ 

In this section, we propose an IND-CCA secure KEM, called EMBLEM, in the quantum random oracle model. To achieve CCA security, the KEM variant on the FO transformation [19] is applied to the present IND-CPA secure encryption scheme EMBLEM.CPA. 

Let _M_ = _{_ 0 _,_ 1 _}_<sup>256</sup> be the message space of EMBLEM. CPA. The system parameter _params_ is given as in EMBLEM.CPA. In the FO transformation, the following three hash functions are required: 

- The hash function _G_ : _{_ 0 _,_ 1 _}_<sup>_∗_</sup> _→{_ 0 _,_ 1 _}_<sup>256</sup> . 

- The hash function _H_ : _{_ 0 _,_ 1 _}_<sup>_∗_</sup> _→{_ 0 _,_ 1 _}_<sup>256</sup> . 

- The hash function _H_<sup>ˆ</sup> : _{_ 0 _,_ 1 _}_<sup>_∗_</sup> _→{_ 0 _,_ 1 _}_<sup>256</sup> . 

These hash functions will be modeled as random oracles in the security proof. The CCA-secure KEM EMBLEM = (KeyGen, Encap, Decap) is constructed as follows: 

Note that all the ephemeral values selected in the EMBLEM _._ CPA _._ Encrypt algorithm are determined by the random _r_ . The correctness of this scheme is derived from that of the underlying CPA-secure PKE scheme [19]. 

**Theorem 3.3 (Correctness).** _If EMBLEM.CPA is (_ 1 _− ϵ)correct, then EMBLEM is (_ 1 _− ϵ)-correct in the quantum random oracle model._ 

EMBLEM is tightly IND-CCA secure in the (classical) random oracle model (by Theorem 3.4) and is non-tightly IND-CCA secure in the quantum random oracle model (by Theorem 3.5). Note that the hash function _H_<sup>ˆ</sup> is not required to prove IND-CCA security in the (classical) random oracle model. 

**Theorem 3.4 (Theorems 2 and 3 in [19]).** _It is assumed that EMBLEM.CPA is δ-correct. For any IND-CCA adversary B issuing at most qD decryption queries, at most qG queries to the random oracle G, and at most qH queries to the random oracle H, there exists an IND-CPA adversary A such that_ 

_and the running time of A is approximately that of B._ 

**Theorem 3.5 (Theorems 8 and 9 in [19]).** _It is assumed that EMBLEM.CPA is δ-correct. For any IND-CCA quantum adversary B issuing at most qD (classical) decryption queries, at most qG queries to the quantum random oracle G, at most qH queries to the quantum random oracle H, and at most q_ ˆ _H_<sup>_queriestothequantumrandomoracleH_ˆ</sup><sup>_,thereexistsan_</sup> _IND-CPA quantum adversary A such that_ 

_and the running time of A is approximately that of B._ 

## IV. RING VARIANT OF EMBLEM 

KeyGenSame(1<sup>_λ_</sup> ).as EMBLEM _._ CPA _._ KeyGen. 

EncapTo( _pk_ generate). a key _K_ and a ciphertext _C_ , proceed as follows: 

   - 1) Select a random string _δ ←{_ 0 _,_ 1 _}_<sup>256</sup> and compute _r_ = _G_ ( _δ_ ). 

   - 2) Compute _C_ 1 _←_ EMBLEM _._ CPA _._ Encrypt( _pk, δ_ ; _r_ ) and _C_ 2 = _H_<sup>ˆ</sup> ( _δ_ ). 

   - 3) Compute _K_ = _H_ ( _δ, C_ 1 _, C_ 2). 

   - 4) Return the ciphertext _C_ = ( _C_ 1 _, C_ 2) _∈_ Z<sup>_v_</sup> _q_<sup>_×_(</sup><sup>_n_+</sup><sup>_k_)</sup> _×{_ 0 _,_ 1 _}_<sup>256</sup> and the key _K∈{_ 0 _,_ 1 _}_<sup>256</sup> . 

- DecapParse( _sk, C_ the). ciphertext _C_ as ( _C_ 1, _C_ 2), and proceed as follows: 

   - 1) Compute _δ ←_ EMBLEM _._ CPA _._ Decrypt( _sk, C_ 1). 

   - 2) Compute _r_ = _G_ ( _δ_ ). 

   - 3) Compute _e ←_ EMBLEM _._ CPA _._ Encrypt( _pk, δ_ ; _r_ ) and _d_ = _H_<sup>ˆ</sup> ( _δ_ ). 

In this section, we propose a ring variant of EMBLEM, called R.EMBLEM. An IND-CPA secure PKE scheme over rings (R.EMBLEM.CPA is first constructed in Section IV-A), and it is converted into an IND-CCA secure KEM (R.EMBLEM in Section IV-B) using the KEM variant of the FO transformation [19]. 

## _A. R.EMBLEM.CPA_ 

## _(CPA-Secure PKE over Rings)_ 

_•_ **Encoding and decoding functions over rings.** Let _M_ = _{_ 0 _,_ 1 _}_<sup>_ℓ_</sup> be the message space. The encoding function **R.encode** , which takes as input a bit string and outputs a polynomial, and its inverse function **R.decode** are defined. **R.encode** operates similarly to the **encode** function in Section III-A, except that the output is in polynomial form over rings. For an _ℓ_ -bit message _M_ , a block size _t_ , an upper 

8 

bound _d_ for the error size, and a modulus _q_ , **R.encode** and **R.decode** operate as follows: 

## ▶ **R.encode** ( _M, t, d, q_ ) 

- 1) Split the message into _t_ -bit blocks (it is assumed that _t_ divides _ℓ_ ) and generate message blocks _{Mi}_ for _i ∈_ [1 _, ℓ/t_ ]. 

- 2) Output a polynomial _m_ ˆ whose _i_ -th coefficient is computed as _m_ ˆ _i ← Mi||_ 1 _||_ 0<sup>_d_</sup> for _i ∈_ [1 _, ℓ/t_ ] (as in Fig. 4). 

<!-- Start of picture text -->
���� ����<br>��� �� �� �������<br>����� ����� �����<br>�� ��� � ��� � ��� ��� � ��� ��� ����� � �����<br><!-- End of picture text -->

Fig. 4: Encoding function **R.encode** 

## ▶ **R.decode** ( ˆ _m, t, q_ ) 

- ˆ ˆ 

- 1) Compute _Mi_ = [ _mi_ ] _t_ for _i ∈_ [1 _, l/t_ ], where _mi_ is the _i_ -th of _m_ ˆ . 

- 2) Output _ℓ_ -bit string _M_ = _M_ 1 _|| · · · ||Ml/t_ . 

For the cyclotomic polynomial _f_ ( _x_ ) = _x_<sup>_n_</sup> + 1 _∈_ Z[ _x_ ], let _Rq_ = Z _q_ [ _x_ ] _/⟨f_ ( _x_ ) _⟩_ be the ring of integer polynomials modulo both _f_ ( _x_ ) and _q_ . An element of _Rq_ can be represented by a polynomial of degree less than _n_ whose coefficients are in Z _q_ . 

_•_ **Truncate function.** To improve transmission efficiency, a function Trunc is used that truncates the ciphertext [49]. Let _a ∈ Rq_ be a polynomial of degree _n_ , represented by _a_ = _a_ 1 + _a_ 2 _X_ + _· · ·_ + _anX_<sup>_n−_1</sup> . For 1 _≤ ℓ ≤ n_ , Trunc is defined as follows: 

_•_ **Scheme.** Given a security parameter 1<sup>_λ_</sup> , the system-wide parameter _params_ is generated as follows: Positive integers _n_ , _t_ and the modulus _q_ are chosen. A standard deviation _σ_ = _s/√_ 2 _π_ for _GDs_ and a positive integer _B < σ_ are chosen. Let _Rq_ denote the reduction of a ring _R_ modulo _q_ , i.e., _Rq_ = Z _q_ [ _X_ ] _/⟨f_ ( _x_ ) _⟩_ . Then, _params_ is given by ( _n, t, q, B, Rq, GDs_ ). It is assumed that _params_ is used for all algorithms in R.EMBLEM.CPA. 

- KeyGenChoose(1<sup>_λ_</sup> ). a random polynomial _a ← Rq_ , a secret polynomial _x ∈ Rq_ where the coefficient vector is sampled randomly from [ _−B, B_ ]<sup>_n_</sup> .<sup>3</sup> Choose an error polynomial _e ∈ Rq_ , where the coefficient of _e_ is sampled randomly from _GD_<sup>_n_</sup> _s_<sup>.Compute</sup><sup>_b_</sup> = _a · x_ + _e ∈ Rq_ . The key pair ( _pk, sk_ ) is given by _pk_ = ( _a, b_ ) and _sk_ = ( _x_ ). 

- EncryptLet( _pk, Mℓ_ = 256 _∈M < n_ ). . To encrypt a message, proceed as follows: 

> 3As in EMBLEM.CPA, the coefficients of _x_ can be generated using PRF, and thus it is possible to store only a seed. 

- 1) _m_ ˆ _←_ **R.encode** ( _M, t, d, q_ ). 

- 2) Choose a random _z ∈{_ 0 _,_ 1 _}_<sup>256</sup> . 

- 3) ( _r, e_ 1 _, e_ 2) _←_ **Sam** ( _z_ ), where _r_ and ( _e_ 1 _, e_ 2) are polynomials whose coefficients are sampled from [ _−B, B_ ] and _GDs_ , respectively. 

- 4) Compute _c_ 1 = _r · a_ + _e_ 1 and _c_ 2 _←_ Trunc( _r · b_ + _e_ 2 _, ℓ/t_ ) + _m_ ˆ . 

- 5) Return the ciphertext _c_ = ( _c_ 1 _, c_ 2) _∈ Rq_<sup>2.</sup> 

DecryptParse( _sk, c_ the). ciphertext _c_ as ( _c_ 1 _, c_ 2). 

- 1) Compute _d ←_ Trunc( _c_ 1 _· x, ℓ/t_ ). 

- ˆ 

- 2) Compute _m_ = _c_ 2 _− d_ . 3) Output _M ←_ **R.decode** ( ˆ _m, t, q_ ). 

**Theorem 4.1 (Correctness).** _Let_ ( _r · e_ )<sup>(</sup><sup>_i_)</sup> _, e_ 2<sup>(</sup><sup>_i_)</sup> _, and_ ( _e_ 1 _· x_ )<sup>(</sup><sup>_i_)</sup> _be the i-th coefficients of the polynomials_ ( _r·e_ ) _, e_ 2 _, and_ ( _e_ 1 _·x_ ) _. Let ϵ_ = Pr � _i∈_ max[1 _,ℓ/t_ ] � _|_ ( _r · e_ )<sup>(</sup><sup>_i_)</sup> _|_ + _|e_<sup>(</sup> 2<sup>_i_)</sup><sup>_|_+</sup><sup>_|_(</sup><sup>_e_1</sup><sup>_· x_)(</sup><sup>_i_)</sup><sup>_|_</sup> � _≥_ 2<sup>_d_�</sup> , _where d_ = _⌊_ log _q⌋−_ ( _t_ +1) _. Then R.EMBLEM.CPA is_ (1 _−ϵ_ ) _- correct._ 

_Proof._ For the ciphertext _c_ , let ( _c_ 1 _, c_ 2) be _c_ 1 = _r · a_ + _e_ 1, ˆ _c_ 2 _←_ Trunc( _r · b_ + _e_ 2 _, ℓ/t_ ) + _m_ . In the decryption phase, 

The _i_ -th coefficient of _m_ ˆ is encoded as _Mi||_ 1 _||_ 0<sup>_d_</sup> for _i ∈_ [1 _, ℓ/t_ ], where _Mi ∈{_ 0 _,_ 1 _}_<sup>_t_</sup> is the _i_ -th message block of _M_ . Therefore, if the absolute size of _i_ -th coefficient in ( _re_ + _e_ 2 _− e_ 1 _x_ ) is less than 2<sup>_d_</sup> , it does not affect the message _Mi_ . As a result, the decryption of R _._ EMBLEM _._ CPA performs correctly with probability 1 _− ϵ_ . □ 

**Theorem 4.2 (Security).** R _._ EMBLEM _._ CPA _is IND-CPA secure if the smaRLWEn,q assumption holds._ 

_Proof._ The proof proceeds with a sequence of games, as in Theorem 3.2. In Game 0, the public key is a small secret ring-LWE instance and the ciphertext is an encryption of _M_ 0. In Game 7, the public key is a small secret ring-LWE instance, and the ciphertext is an encryption of _M_ 1. Let _Di_ denote the distribution of Game _i_ . It will be shown that _D_ 0 and _D_ 7 are computationally indistinguishable for the adversary. 

_▷_ **Game 0.** This is the real game, where the ciphertext is an encryption of _M_ 0. Note that _m_ ˆ0 _←_ **R.encode** ( _M_ 0 _, t, d, q_ ). _D_ 0 is given as follows: 

_▷_ **Game 1.** In Game 1, the public key _b_ is uniformly generated at random. The remaining part is the same as in Game 0. _D_ 1 is given as follows: 

_▷_ **Game 2.** In Game 2, _c_ 1 is changed into a random polynomial. The remaining part is the same as in Game 1. _D_ 2 is given as follows: 

9 

ˆ _C ←_ <u>(</u> _c_ 1 = _u_ 1 _, c_ 2 = Trunc( _r ·b_ + _e_ 2 _, ℓ/t_ )+ _m_ 0) _}_ . 

_▷_ **Game 3.** In Game 3, the (truncated) _c_ 2 is changed into a random polynomial. The remaining part is the same as in Game 2. _D_ 3 is given as follows: 

_▷_ **Game 4.** In Game 4, the message _M_ 0 is changed into _M_ 1. Note that _m_ ˆ1 _←_ **R.decode** ( _M_ 1 _, t, q_ ). The remaining part is the same as in Game 3. _D_ 4 is given as follows: 

_▷_ **Game 5.** In Game 5, _c_ 2 is restored to the small secret ringLWE instance, and the remaining part is the same as in Game 4. _D_ 5 is given as follows: 

_▷_ **Game 6.** In Game 6, _c_ 1 is restored to the small secret ringLWE instance, and the remaining part is the same as in Game 5. _D_ 6 is given as follows: 

_▷_ **Game 7.** In Game 7, _b_ is restored to the small secret ringLWE instance, and the remaining part is the same as in Game 6. _D_ 7 is given as follows: 

_D_ 0 and _D_ 1 are computationally indistinguishable under the smaRLWE _n,q_ assumption. _D_ 1 and _D_ 2 are also computationally indistinguishable under the smaRLWE _n,q_ assumption. _D_ 2 and _D_ 3 are computationally indistinguishable under the smaRLWE _n,q_ assumption. In Games 3 and 4, the ciphertexts are computed in a one-time pad manner, and thus _D_ 3 and _D_ 4 are statistically indistinguishable. The hybrid games, i.e., Games 4–7, proceed as in the case of Games 0–3, but in a reverse manner. If the parameters _n_ and _q_ of smaRLWE are set to be as hard as RLWE, then the security of the ring LWE problem can be reduced to that of R.EMBLEM.CPA. □ 

## _B. R.EMBLEM_ 

## _(CCA-Secure KEM over Rings)_ 

In this section, we propose an IND-CCA secure KEM, called R.EMBLEM, in the quantum random oracle model. To achieve CCA security, the KEM variant on the FO transformation [19] is applied to R.EMBLEM.CPA. 

Let _M_ = _{_ 0 _,_ 1 _}_<sup>256</sup> be the message space of R.EMBLEM .CPA. The system parameter _params_ is given as in R.EMBLEM.CPA, except that the hash functions _G, H, H_<sup>ˆ</sup> described in Section III-B are additionally included. R.EMBLEM = (KeyGen, Encap, Decap) is constructed as follows: 

- EncapLet( _pk_ ) _n >_ . 256. To generate a key _K_ and a ciphertext _c_ , proceed as follows: 

   - 1) Select a random string _δ ←{_ 0 _,_ 1 _}_<sup>256</sup> and compute _z_ = _G_ ( _δ_ ). 

   - 2) Compute ( _c_ 1 _, c_ 2) _←_ R _._ EMBLEM _._ CPA _._ Encrypt( _pk, δ_ ; _z_ ) and _c_ 3 = _H_<sup>ˆ</sup> ( _δ_ ). 

   - 3) Compute _K_ = _H_ ( _δ, c_ 1 _, c_ 2 _, c_ 3). 

DecapParse( _sk, c_ )the. ciphertext _c_ as ( _c_ 1 _, c_ 2 _, c_ 3) and proceed as follows: 

- 1) Compute _δ ←_ R _._ EMBLEM _._ CPA _._ Decrypt( _sk,_ ( _c_ 1 _, c_ 2)). 2) Compute _z_ = _G_ ( _δ_ ). 

- 3) Compute ( _d_ 1 _, d_ 2) _←_ R _._ EMBLEM _._ CPA _._ Encrypt( _pk, δ_ ; _z_ ) and _d_ 3 = _H_<sup>ˆ</sup> ( _δ_ ). 

   - If ( _d_ 1 _, d_ 2) = ( _c_ 1 _, c_ 2) or _d_ 3 = _c_ 3, output _⊥_ . 

- 4) Otherwise, output _K_ = _H_ ( _δ, c_ 1 _, c_ 2 _, c_ 3). 

The correctness of this scheme is derived from that of the underlying CPA-secure PKE scheme. 

**Theorem 4.3 (Correctness).** _If R.EMBLEM.CPA is_ (1 _− ϵ_ ) _- correct, then R.EMBLEM is_ (1 _− ϵ_ ) _-correct in the quantum random oracle model._ 

As in EMBLEM, R.EMBLEM is tightly IND-CCA secure in the (classical) random oracle model (by Theorem 4.4) and is non-tightly IND-CCA secure in the quantum random oracle model (by Theorem 4.5). 

**Theorem 4.4 (Theorems 2 and 3 in [19]).** _It is assumed that R.EMBLEM.CPA is δ-correct. For any IND-CCA adversary B issuing at most qD decryption queries, at most qG queries to random oracle G, and at most qH queries to random oracle H, there exists an IND-CPA adversary A such that_ 

## _and the running time of A is approximately that of B._ 

**Theorem 4.5 (Theorems 8 and 9 in [19]).** _It is assumed that R.EMBLEM.CPA is δ-correct. For any IND-CCA quantum adversary B issuing at most qD (classical) decryption queries, at most qG queries to the quantum random oracle G, at most qH queries to the quantum random oracle H, and at most q_ ˆ _H_<sup>_queriestothequantumrandomoracleH_ˆ</sup><sup>_,thereexistsan_</sup> _IND-CPA quantum adversary A such that_ 

_and the running time of A is approximately that of B._ 

## V. PARAMETER SELECTION 

## _A. Distributions_ 

_•_ **Secret key distribution** _D_ **s.** The distribution _Ds_ is uniform distribution over [ _−B, B_ ] for a positive integer _B_ . As the value _B_ decreases, the expected size of the decryption error can also 

10 

decreases. Three cases are considered, where _B_ is set to be 1, 2, and 3. 

_•_ **Error distribution** _D_ **e.** The distribution _De_ is the discrete Gaussian distribution with the Gaussian parameter _s_ , i.e., _GDs_ . For the standard deviation _σ_ = _s/√_ 2 _π_ of _GDs_ , _σ_ is set to 25 for EMBLEM and 3 for R.EMBLEM. 

_•_ **Message space** _M_ **.** _M_ is set to _{_ 0 _,_ 1 _}_<sup>256</sup> (i.e., _ℓ_ = 256). In EMBLEM, _k, t, v_ are set to be positive integers such that _k × t × v_ = _ℓ_ = 256. A message _m ∈{_ 0 _,_ 1 _}_<sup>256</sup> is encoded as a _v × k_ matrix, where each element is associated with a _t_ -bit message block. 

## _B. Probability of Decryption Failure_ 

For simplicity, the case _v_ = 1 is considered in EMBLEM. Then, **R** _∈_ [ _−B, B_ ]<sup>_m_</sup> , **E1** _∈GD_<sup>_n_</sup> _s_<sup>,and</sup><sup>**E2**</sup><sup>_∈GDk_</sup> _s_<sup>.For</sup><sup>_i∈_</sup> [1 _, k_ ], let 

To ensure the **_correctness_** of EMBLEM, the size of the decryption error _d_ should be set so as to satisfy the following equation: 

Once _d_ is determined, for a given _t_ , the size of the modulus _q_ satisfying _⌊_ log _q⌋_ = _t_ + 1 + _d_ should be determined. In the parameter setting (in Section VII), _d_ was set so that the probability _ϵ_ of decryption failure is negligible, namely, _ϵ ≈_ 2<sup>_−_140</sup> , using the following Lemmas: 

**Lemma 5.1** (Lemma 2.4 of [50]). _For any real s >_ 0 _and Q >_ 0 _, and any x ∈_ R<sup>_n_</sup> _, we have_ 

_where the standard deviation σ of GD_ Z _n,s is set to σ_ = _s/√_ 2 _π._ 

**Lemma 5.2** (Lemma 3.3 of [51]). _For any r >_ 0 _and z ←GDs of the standard deviation σ_ = _s/√_ 2 _π, we have_ Pr � _|z| > Tσ_ � _<_ 2 _e_<sup>_−T_2</sup><sup>_/_2</sup> _._ (5) 

By Lemma 5.1, _|⟨_ **R** _, E_<sup>(</sup><sup>_i_)</sup> _⟩|_ and _|⟨_ **E1** _, X_<sup>(</sup><sup>_i_)</sup> _⟩|_ can be determined with probability 2 _e_<sup>_−π·Q_2</sup> as follows: 

_• |⟨_ **R** _, E_<sup>(</sup><sup>_i_)</sup> _⟩| ≤ Q · σ√_ 2 _π · ||_ **R** _||_ . _• |⟨_ **E1** _, X_<sup>(</sup><sup>_i_)</sup> _⟩| ≤ Q · σ√_ 2 _π · ||X_<sup>(</sup><sup>_i_)</sup> _||_ . 

Here, _Q_ is set to approximately 5 _._ 5776, so that the probability 2 _e_<sup>_−π·Q_2</sup> _≈_ 2<sup>_−_140</sup> in Lemma 5.1 (i.e., 2 _e_<sup>_−π·_5</sup><sup>_._57762</sup> _≈_ 2<sup>_−_140</sup> ). By Lemma 5.2, _|e_<sup>(</sup> 2<sup>_i_)</sup><sup>_|_canalsobeestimatedwithprobability</sup> 2 _e_<sup>_−T_2</sup><sup>_/_2</sup> as follows: 

_• |e_<sup>(</sup> 2<sup>_i_)</sup><sup>_| ≤Tσ_.</sup> 

Here, _T_ is set to approximately 13 _._ 98, so that the probability 2 _e_<sup>_−T_2</sup><sup>_/_2</sup> _≈_ 2<sup>_−_140</sup> in Lemma 5.2 (i.e., 2 _e_<sup>_−_13</sup><sup>_._982</sup><sup>_/_2</sup> _≈_ 2<sup>_−_140</sup> ). Therefore, for all _i ∈_ [1 _, k_ ], there is _d_ such that 

where _Q_ = 5 _._ 5776 and _T_ = 13 _._ 98. 

## **Case 1** **<u>(</u>** _B_ = 1 **<u>)</u>** 

If **R** and _X_<sup>(</sup><sup>_i_)</sup> are uniformly sampled from _{−_ 1 _,_ 0 _,_ 1 _}_<sup>_m_</sup> and _{−_ 1 _,_ 0 _,_ 1 _}_<sup>_n_</sup> at random (i.e., _B_ = 1), the expected values of _||_ **R** _||_ and _||X_<sup>(</sup><sup>_i_)</sup> _||_ are � <u>23</u><sup>_m_and</sup> � <u>23</u><sup>_n_,respectively.If</sup> ( _m, n, σ_ ) = (1008 _,_ 824 _,_ 25), each value can be calculated as follows: 

## **Case 2** **<u>(</u>** _B_ = 2 **<u>)</u>** 

If **R** and _X_<sup>(</sup><sup>_i_)</sup> are uniformly sampled from _{−_ 2 _, −_ 1 _,_ 0 _,_ 1 _,_ 2 _}_<sup>_m_</sup> and _{−_ 2 _, −_ 1 _,_ 0 _,_ 1 _,_ 2 _}_<sup>_n_</sup> at random <u>(i.e.,</u> _B_ = 2), the expected values of _||_ **R** _||_ and _||X_<sup>(</sup><sup>_i_)</sup> _||_ are _√_ 2 _m_ and _√_ 2 _n_ , respectively. If ( _m, n, σ_ ) = (1016 _,_ 784 _,_ 25), each value can be calculated as follows: 

**Case 3** **<u>(</u>** _B_ = 3 **<u>)</u>** 

If **R** and _X_<sup>(</sup><sup>_i_)</sup> are uniformly sampled from _{−_ 3 _, −_ 2 _, −_ 1 _,_ 0 _,_ 1 _,_ 2 _,_ 3 _}_<sup>_m_</sup> and _{−_ 3 _, −_ 2 _, −_ 1 _,_ 0 _,_ 1 _,_ 2 _,_ 3 _}_<sup>_n_</sup> at random (i.e., _B_ = 3), the expected values of _||_ **R** _||_ and _||X_<sup>(</sup><sup>_i_)</sup> _||_ are _√_ 4 _m_ and _√_ 4 _n_ , respectively. If ( _m, n, σ_ ) = (936 _,_ 808 _,_ 25), each value can be calculated as follows: 

It should be noticed that the same analysis as above can also be applied to the ring-based constructions R.EMBLEM.CPA and R.EMBLEM. 

## _C. Proposed parameter sets_ 

Table I and II present the parameter settings for EMBLEM and R.EMBLEM, respectively. The dimension _n_ and the number of samples _m_ were set according to Equation (2), and the upper bound _d_ for the decryption error were set based on the above analysis. The parameterization is aimed 

11 

|(_λ_=128)|**FrodoKEM**|**LOTUS**|||**EMB**|**LEM**|||
|---|---|---|---|---|---|---|---|---|
||||**[I]**|**[II]**|**[III]**|**[IV]**|**[V]**|**[VI]**|
|_m_|640|832|1,008|1,008|1,016|1,016|936|936|
|_n_|640|832|824|824|784|784|808|808|
|_k_|8|256|64|32|64|32|64|32|
|log_q_|15|13|20|20|20|20|21|21|
|_σ_|2.75|3|25|25|25|25|25|25|
|_t_|4|1|4|4|4|4|4|4|
|_v_|8|1|1|2|1|2|1|2|
|PK size (bytes)|9,616|658,950|161,312|80,672|162,592|81,312|78,656|39,344|
|SK size (bytes)|10,256|700,420|32|32|32|32|32|32|
|CT size (bytes)|9,736|1,144|2,252|4,312|2,152|4,112|4,442|8,684|
|KeyGen (ms)|0.461|16.393|29.059|19.361|26.053|16.809|27.483|17.502|
|Encap (ms)|0.631|0.151|0.750|1.085|0.563|0.952|0.458|0.797|
|Decap (ms)|0.654|0.179|0.704|0.908|0.546|0.723|0.444|0.625|
|Prob. decryption failure|2<sup>_−_148</sup><sup>_._8</sup>|2<sup>_−_256</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|
|Security level|128|128|129.3|129.3|129.6|129.6|129.1|129.1|
|Secret distribution|_GDs_|_GDs_|_U_[_−_1_,_1]|_U_[_−_1_,_1]|_U_[_−_2_,_2]|_U_[_−_2_,_2]|_U_[_−_3_,_3]|_U_[_−_3_,_3]|
|Hardness problem|LWE|LWE|LWE|LWE|LWE|LWE|LWE|LWE|

Table I: Comparison of CCA-secure KEMs 

at achieving at least 128-bit security level and a probability 2<sup>_−_140</sup> of decryption failure. Particularly, to estimate the exact security level for each parameter setting, the sage module [10] 4 was used, which is designed for estimating the concrete hardness of a given LWE (or LWR) instance. 

## _A. Secret Key Generation_ 

In EMBLEM, the secret key matrix **X** _∈_ [ _−B, B_ ]<sup>_n×k_</sup> is generated as follows. First, the randombytes() function is called to generate octet strings rnd[ ] of length _n × k_ . Subsequently, each string is divided by 2 _B_ + 1 and _B_ + 1 is subtracted from the remainder so that the result is in [ _−B, B_ ]. If _B_ = 1, the _i_ -th row and _j_ -th column of **X** , denoted by _Xi,j_ , is computed by _Xi,j ←_ (rnd[ _i, j_ ]%3) _−_ 2, and the result will be in [ _−_ 1 _,_ 1]. In R.EMBLEM, the secret key is a polynomial _x_ of degree _N_ . Thus, the randombytes() function is called to generate octet strings rnd[ ] of length _N_ , and the remaining part is the same as above. The _i_ -th coefficient of **X** , denoted by _X_<sup>ˆ</sup> _i_ , is computed by _X_<sup>ˆ</sup> _i ←_ (rnd[ _i_ ]%(2 _B_ + 1)) _−_ ( _B_ + 1) for _i ∈_ [0 _, N −_ 1]. 

## _B. Discrete Gaussian Sampler_ 

An inversion sampling method is used for instantiating the discrete Gaussian sampler, and the table look-up method is used for resistance against side-channel attacks. For _σ_ = 3, the cumulative density table (CDT), denoted by _T_ , is created from probability density functions. Nine bits are uniformly 

## _C. Sampling Function_ 

The sampling function **Sam** (in EMBLEM) takes a 256bit string _r_ as input and outputs **R** _∈_ [ _−B, B_ ]<sup>_m×v_</sup> and ( **E1** _,_ **E2** ) _∈ GDs_<sup>_v×_(</sup><sup>_n_+</sup><sup>_k_)</sup> . The matrix **R** is generated as follows: for simplicity, it is assumed that _B_ = 1. First, the hash function BLAKE512 with input _r_ is computed to obtain digest, whose data type is int. Let _len_ be the number of int arrays of digest, and let digest[ _i_<sup>_′_</sup> ] denote the _i_<sup>_′_</sup> -th index of digest. For _i ∈_ [1 _, m_ ] and _j ∈_ [1 _, v_ ], the _i_ -th row and _j_ -th column of **R** , denoted by **R** _i,j_ , is computed as follows: 

When digest[ _i_<sup>_′_</sup> ] becomes zero, the next index is considered and the process is repeated. When all indexes have been processed, BLAKE512 is computed with input _r_ + 1 and the process is repeated again. When all _m × v_ elements of **R** are filled, the process is terminated. At the end of the process, the original _r_ is hashed again (using SHA256), and the result is used as a new _seed_ to generate the error matrices **E1** _,_ **E2** . The 

4https://bitbucket.org/malb/lwe-estimator 

12 

|(_λ_=128)|**NewHope**|**Kyber**||**R.EM**|**BLEM**||
|---|---|---|---|---|---|---|
||||**[I]**|**[II]**|**[III]**|**[IV]**|
|_n_|1024|256|1,024|1,024|1,024|1,024|
|_k_|1|3|1|1|1|1|
|log_q_|14|13|14|16|14|16|
|log_p_|-|9|-|-|-|-|
|_σ_|_√_<br>8|_√_<br>2|3|3|3|3|
|_t_|1|1|1|2|1|2|
|PK size (bytes)|1,824|1,088|1,824|2,080|1,824|2,080|
|SK size (bytes)|3,680|2,400|32|32|32|32|
|CT size (bytes)|2,208|1,152|2,272|2,336|2,272|2,336|
|KeyGen (ms)|0.432|0.315|0.124|0.307|0.122|0.295|
|Encap (ms)|0.635|0.419|0.152|0.276|0.149|0.272|
|Decap (ms)|0.721|0.512|0.217|0.492|0.219|0.417|
|Prob. decryption failure|2<sup>_−_216</sup>|2<sup>_−_142</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|2<sup>_−_140</sup>|
|Security level|233|161|219.2|181.3|236.6|193.5|
|Secret distribution|Z_q_|_Bη_|_U_[_−_1_,_1]|_U_[_−_1_,_1]|_U_[_−_2_,_2]|_U_[_−_2_,_2]|
|Hardness problem|Ring-LWE|Module-LWE|Ring-LWE|Ring-LWE|Ring-LWE|Ring-LWE|

Table II: Comparison of CCA-secure KEMs over Rings 

pseudocode for generating the matrix **R** and the _seed_ is given in Algorithm 2. **E1** and **E2** are generated using a discrete Gaussian sampler with _seed_ as input. In Algorithm 1, _seed_ is additionally used as a fixed input to the rand() function, which allows obtaining the same output **E1** and **E2** for the same _seed_ . 

**Algorithm 2:** Generating the matrix **R** and the _seed_ 

**Input :** 256-bit string _r_ **Output:** matrix **R** _∈_ [ _−_ 1 _,_ 1]<sup>_m×v_</sup> , _seed_ 

**1** _cnt ←_ 0; 

_F_ = Z _p_ , i.e., integers modulo the prime _p_ , or a ring. In R.EMBLEM, NTT is based on the Cooly–Tukey butterfly, and the inverse NTT (INTT) is based on the Gentleman– Sande butterfly. Let _ψ_ be a primitive 2 _n_ -th root of unity, _ψ_<sup>1024</sup> _≡_ 1 mod _q_ , such that _ψ_<sup>2</sup> = _w_ , where _w_<sup>512</sup> _≡_ 1 mod _q_ . We generate the _ψ_ and _ψ_<sup>_−_1</sup> tables in bit reversed order. For _p_ = 12289, _ψ_ = 1987 was used, and for _p_ = 40961, _ψ_ = 1044 was used. Note that the NTT and INTT tables are known to public. 

## _D. Number Theoretic Transform_ 

The Number Theoretic Transform (NTT) is a specialized version of the discrete Fourier transform in the